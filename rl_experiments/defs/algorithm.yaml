# Definition of a specific algorithm

name: algorithm   # algorithm unique name
version: version   # algorithm version
repository: repository  # algorithm source code
commit: hash   # hash of the last commit
diff: ""   # an optional path to a git patch file (or empty string if worktree is clean)
params: {}  # a dict of parameters
